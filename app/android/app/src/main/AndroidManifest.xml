<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Atrium talks to self-hosted services over the network. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Optional biometric unlock (opt-in, gated in-app). -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>

    <application
        android:label="${appName}"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:allowBackup="false"
        android:fullBackupContent="false">
        <!-- usesCleartextTraffic: self-hosted LAN services are commonly plain
             http:// on a private IP. allowBackup=false keeps credential
             material out of cloud/adb backups. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Flutter otherwise feeds the launch intent's data URI to the
                 router as the initial route, so opening a magnet link landed
                 the app on "no routes for location: magnet:?xt=...". Atrium
                 receives no deep links of its own - the torrent intents below
                 are read natively and handed to the share channel - so nothing
                 needs this. -->
            <meta-data
              android:name="flutter_deeplinking_enabled"
              android:value="false"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Torrent hand-off from other apps. Without these Atrium cannot
                 appear in the chooser at all: Android only offers activities
                 whose filters match the intent being resolved.

                 application/octet-stream is deliberately not matched. Some file
                 managers do label .torrent that way, but claiming it would put
                 Atrium in the chooser for every unknown binary, and pathPattern
                 cannot narrow it because content URIs often carry no filename.
                 file:// is likewise absent: Android 7+ raises
                 FileUriExposedException when one app passes another a file URI,
                 so it cannot arrive. -->
            <!-- Tapping a downloaded .torrent. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:host="*"
                    android:mimeType="application/x-bittorrent"
                    android:scheme="content"/>
            </intent-filter>
            <!-- Magnet links opened from a browser. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="magnet"/>
            </intent-filter>
            <!-- Sharing a magnet or a link to a .torrent as text. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
            <!-- Sharing a .torrent file itself. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="application/x-bittorrent"/>
            </intent-filter>
            <!-- Sharing several at once. Without this Atrium disappears from
                 the chooser the moment a second file is selected, which is the
                 normal way people move a batch off a tracker. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="application/x-bittorrent"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <package android:name="com.mb.android" />
        <package android:name="tv.emby.embyatv" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="emby" />
        </intent>
        <package android:name="org.jellyfin.mobile" />
        <package android:name="org.jellyfin.androidtv" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="jellyfin" />
        </intent>
        <package android:name="com.plexapp.android" />
        <!-- Android 11+ package visibility: needed for url_launcher to resolve
             a browser for the About screen's source-code link. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>
</manifest>
